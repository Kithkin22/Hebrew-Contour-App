/**
 * UI shell disabled (redesign rolled back).
 * Stub kept so cached index.html that still references this file cannot mount the three-column layout.
 */
(function () {
  'use strict';
  /* intentionally empty */
})();
